#!/usr/bin/env node

import process from "node:process";

console.error(
  [
    "create_own_module_list.ts is deprecated and no longer maintained.",
    "Use the canonical pipeline and fixture tooling instead."
  ].join("\n")
);

process.exit(1);
